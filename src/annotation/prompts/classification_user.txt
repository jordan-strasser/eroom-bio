Classify this trial's outcome by reasoning through the mechanistic causal chain.

## Trial Extraction
- NCT ID: {trial_id}
- Compound: {compound_name}
- Target: {target_name}
- Primary Endpoint: {primary_endpoint}
- Primary Endpoint Met: {primary_endpoint_met}
- Effect Size: {effect_size}
- P-value: {p_value}

## Per-Arm Outcomes (from results_by_chain)
{per_arm_outcomes}

## Valid arm_ids for `affecting_arm_id` (use EXACTLY one of these keys verbatim — NEVER invent your own)
{arm_id_menu}

The `affecting_arm_id` field on every causal-chain edge update MUST be one of the keys above (or `null` for trial-shared edges like `endpoint_captures` / `responds_differently`). The attributor drops any update whose `affecting_arm_id` doesn't match a key in this list — there is no fuzzy fallback. If you cannot confidently map a result to one of these arm_ids, emit `affecting_arm_id: null` rather than guessing.

## Biomarker & Target Engagement Data
{biomarker_data}

## Safety Signals (narrative)
{safety_signals}

## Adverse Events (structured, with per-arm incidence)
{adverse_events}

## Dose / Exposure
{dose_info}

## Subgroup Findings
{subgroup_findings}

## Full Extraction Summary
{summary}

## Resolved Graph Entities for This Trial
{graph_entities}

When emitting `edges_to_update`, use ONLY the IDs listed above as `source_entity` and `target_entity`. Do NOT invent free-text names—the attributor drops any edge whose endpoints don't match a node in the trial subgraph.

If you would have referenced a target/biology/population that isn't listed here, that update probably doesn't belong on this trial—omit it rather than substitute.

**`source_entity` must be a SINGLE canonical compound id from the "Per-arm causal chains" block, never a concatenated regimen name.** For a combo arm whose constituents are `carboplatin` + `paclitaxel`, emit TWO separate `affects` updates — one per constituent — NOT a single `affects: carboplatin_paclitaxel → ...`. The attributor drops concatenated compound ids because they don't correspond to any chain. Same rule applies to the `modulator_compound_id` and `primary_compound_id` fields in `modulation_entries`: each must be one canonical compound id, never a joined string.

Return ONLY the JSON classification.